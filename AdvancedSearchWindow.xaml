<Window x:Class="get_link_manga.AdvancedSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ADVANCED SEARCH" Height="320" Width="550"
        Background="{DynamicResource CyberpunkDarkBrush}"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>    <!-- TextBoxes Grid -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Include column -->
            <StackPanel Grid.Column="0" Orientation="Vertical">
                <TextBlock Name="lblInclude" Text="INCLUDE" Foreground="{DynamicResource CyberpunkCyanBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBox Name="txtInc1" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
                <TextBox Name="txtInc2" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
                <TextBox Name="txtInc3" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
                <TextBox Name="txtInc4" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
                <TextBox Name="txtInc5" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
            </StackPanel>

            <!-- Exclude column -->
            <StackPanel Grid.Column="2" Orientation="Vertical">
                <TextBlock Name="lblExclude" Text="EXCLUDE" Foreground="{DynamicResource CyberpunkPinkBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBox Name="txtExc1" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
                <TextBox Name="txtExc2" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
                <TextBox Name="txtExc3" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
                <TextBox Name="txtExc4" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
                <TextBox Name="txtExc5" Style="{StaticResource SurfaceTextBox}" Margin="0,0,0,6"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="btnApply" Content="APPLY" Click="BtnApply_Click" Style="{StaticResource CyberpunkButtonCyan}" Width="90" Height="26" Margin="0,0,8,0" FontWeight="Bold"/>
            <Button Name="btnClear" Content="CLEAR" Click="BtnClear_Click" Style="{StaticResource CyberpunkButtonCyan}" Width="90" Height="26" Margin="0,0,8,0"/>
            <Button Name="btnCancel" Content="CANCEL" Click="BtnCancel_Click" Style="{StaticResource CyberpunkButtonPink}" Width="90" Height="26"/>
        </StackPanel>
    </Grid>
</Window>
